# Core dependencies for governance-layer
# The proxy requires httpx for upstream API forwarding.
# The dashboard and most scripts use only the Python stdlib.
# scripts/verify-qa-chain.py loads specs/qa-chain-events-v1.yaml (QS-031).
httpx>=0.27.0
cryptography>=43.0.0
PyYAML>=6.0
