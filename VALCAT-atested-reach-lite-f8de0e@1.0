#!/usr/bin/env bash
# VALCAT-atested-reach-lite-f8de0e@1.0 -- complete validator catalog runner.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "${SCRIPT_DIR}/scripts/valcat_cli.py" "$@"
