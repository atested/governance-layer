[pytest]
testpaths = tests
markers =
    shell: Shell-based contract tests (may require specific environment setup)
    system: System-level integration tests
    slow: Tests that take more than 10 seconds
